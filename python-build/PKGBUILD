# Maintainer: Christoph Reiter <reiter.christoph@gmail.com>

_realname=build
pkgname="python-${_realname}"
pkgver=1.5.0
pkgrel=1
pkgdesc="A simple, correct Python build frontend"
arch=('any')
url='https://github.com/pypa/build'
msys2_repository_url='https://github.com/pypa/build'
msys2_references=(
  "purl: pkg:pypi/build"
)
license=('spdx:MIT')
depends=("python-packaging"
         "python-pyproject-hooks")
makedepends=(
  "python-flit-core"
  "python-installer"
)
source=("https://pypi.org/packages/source/${_realname::1}/${_realname}/${_realname}-${pkgver}.tar.gz")
sha256sums=('302c22c3ba2a0fd5f3911918651341ebb3896176cbdec15bd421f80b1afc7647')

build() {
  cd "${srcdir}/${_realname}-${pkgver}"

  python -m flit_core.wheel
}

package() {
  cd "${srcdir}/${_realname}-${pkgver}"

  python -m installer --destdir="${pkgdir}" dist/*.whl

  install -D -m644 LICENSE "${pkgdir}"/usr/share/licenses/python-${_realname}/LICENSE
}
