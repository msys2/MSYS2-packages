# Maintainer: Alexey Pavlov <alexpux@gmail.com>

_perlname='Return-MultiLevel'
_realname="${_perlname,,}"
pkgname=perl-${_realname}
pkgver=0.08
pkgrel=2
pkgdesc="Return across multiple call levels"
arch=('any')
url="https://metacpan.org/dist/Return-MultiLevel"
msys2_references=(
  'purl: pkg:cpan/PLICEASE/Return-MultiLevel'
)
groups=('perl-modules')
license=('spdx:Artistic-1.0-Perl' 'spdx:GPL-1.0-or-later')
depends=('perl-data-munge')
checkdepends=('perl-test-fatal')
provides=("perl-Return-MultiLevel=${pkgver}")
conflicts=('perl-Return-MultiLevel')
replaces=('perl-Return-MultiLevel')
options=('!emptydirs')
source=("https://cpan.metacpan.org/authors/id/P/PL/PLICEASE/${_perlname}-${pkgver}.tar.gz")
sha256sums=('51b1aef30c5c4009f640267a08589212e87dcd101800f0d20f9c635c9ffe88a1')

build() {
  cd  "${srcdir}/${_perlname}-${pkgver}"
  perl Makefile.PL INSTALLDIRS=vendor
  make
}

check() {
  cd  "${srcdir}/${_perlname}-${pkgver}"
  make test
}

package() {
  cd  "${srcdir}/${_perlname}-${pkgver}"
  make install DESTDIR="${pkgdir}"

  find ${pkgdir} -name '.packlist' -delete
  find ${pkgdir} -name '*.pod' -delete
}
