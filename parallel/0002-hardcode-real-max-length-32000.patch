diff --git a/src/parallel b/src/parallel
--- a/src/parallel
+++ b/src/parallel
@@ -15333,9 +15333,5 @@ sub real_max_length() {
-    # Find the max_length of a command line
-    # Returns:
-    #	The maximal command line length with 1 byte arguments
-    # return find_max(" c");
-    return find_max("c");
+    return 32000;
 }
 
 sub find_max($) {
